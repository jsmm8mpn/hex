svg(id="hex-tile", width="1000px", height="1000px", version="1.1", xmlns="http://www.w3.org/2000/svg")
  polygon(ng-repeat="vertice in vertices", vertice="vertice")
  //.tileDir(ng-repeat="vertice in vertices", vertice="vertice")
  //defs
    pattern(id="hex-tile", patternUnits="userSpaceOnUse")

  //rect(id="hex-grid", fill="url(#hex-tile)")