svg(id="hex-tile", version="1.1", xmlns="http://www.w3.org/2000/svg")
  //script(src="/ext/SVGPan.js")
  g(id="viewport", transform="matrix(1,0,0,1,-50,-50)", panzoom)
    rect(x="0",y="0",width="{{boardSize[0]+100}}",height="{{boardSize[1]+100}}")
    polygon(ng-repeat="vertice in vertices", vertice="vertice.vertice", water="vertice.water")
    //text(ng-repeat="vertice in vertices", x="{{vertice[2][0]}}", y="{{vertice[2][1]}}") {{$index}}
  //.tileDir(ng-repeat="vertice in vertices", vertice="vertice")
  //defs
    pattern(id="hex-tile", patternUnits="userSpaceOnUse")

  //rect(id="hex-grid", fill="url(#hex-tile)")